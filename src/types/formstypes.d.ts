export type LOGIN_FORM = {
  email: string;
  password: string;
};

export type REGISTER_FORM = {
  name: string;
  username: string;
  email: string;
};

export type PASSWORD_FORM = {
  password: string;
  confirm_password: string;
};